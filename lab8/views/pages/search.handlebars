<div id="searchByTerm">
  <h1>{{ title }}</h1>
  <h2> {{ searchTerm }}</h2>

  Click to: <a href="/">Make another search</a>
  {{#each searchList}}  
    <ul>
      <li><a href="/shows/{{show.id}}">{{show.name}}</a></li>
    </ul>
  {{/each}}
</div>